---
description: Запрет @apply недоступных утилит в CSS-модулях
globs: "**/*.{module.css,css}"
alwaysApply: false
---

# CSS modules: осторожно с `@apply`

В `*.module.css` **нельзя** `@apply`:
- анимации `animate-in`, `fade-in`, `slide-in-from-*` и т.п.
- семантические theme-классы (`bg-background`, `text-muted-foreground`, …)

Их пиши в `className` в TSX. В module CSS — `zinc-*` / `var(--…)`.
